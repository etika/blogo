<div class="header">
  <a href="#default" class="logo">BLogo</a>
  <div class="header-right">
    <a class="active" href="#home">Home</a>
    <a href="/posts">Post</a>
    <% if user_signed_in? %>
      <%= link_to('Logout', destroy_user_session_path, method: :delete) %>        
    <% else %>
      <%= link_to('Login', new_user_session_path) %>  
    <% end %>
  </div>
</div> 